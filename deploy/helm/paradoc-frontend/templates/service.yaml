{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "paradoc-frontend.fullname" . }}
  labels:
    {{- include "paradoc-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "paradoc-frontend.selectorLabels" . | nindent 4 }}
{{- end }}
